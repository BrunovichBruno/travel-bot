print("[wiki_prompts] === ЗАГРУЖЕН ===")

PROMPT_FOOD = """Ты — автор телеграм-канала "Тут и Там" о путешествиях.
Напиши статью про блюдо на основе текста из Википедии.

Стиль: вкусно, живо.
Начни с яркого вступления, расскажи о блюде, откуда оно, из чего готовится.
Добавь 1-2 интересных факта.
В конце — вопрос «А вы пробовали?».
Длина: 1200–1800 символов.
Внутри строк НЕ используй двойные кавычки.

Верни ТОЛЬКО валидный JSON. Первый символ {{, последний }}.
Формат: {{"title": "заголовок", "text": "текст", "teaser": "1-2 предложения"}}

Заголовок: {title}
Текст: {body}
"""

PROMPT_LANDMARK = """Ты — автор телеграм-канала "Тут и Там" о путешествиях.
Напиши статью про достопримечательность.

Стиль: вдохновляюще.
Начни с яркого вступления, расскажи что это, где, чем знаменито.
Добавь 1-2 интересных факта или легенды.
В конце — вопрос «Хотели бы увидеть?».
Длина: 1200–1800 символов.
Внутри строк НЕ используй двойные кавычки.

Верни ТОЛЬКО валидный JSON. Первый символ {{, последний }}.
Формат: {{"title": "заголовок", "text": "текст", "teaser": "1-2 предложения"}}

Заголовок: {title}
Текст: {body}
"""

PROMPT_FACT = """Ты — автор телеграм-канала "Тут и Там" о путешествиях.
Напиши статью с интересными фактами.

Стиль: удивляюще.
Начни с яркого вступления, расскажи 3-5 фактов по теме.
Сохрани все числа, даты, имена.
В конце — вопрос «Знали об этом?».
Длина: 1000–1600 символов.
Внутри строк НЕ используй двойные кавычки.

Верни ТОЛЬКО валидный JSON. Первый символ {{, последний }}.
Формат: {{"title": "заголовок", "text": "текст", "teaser": "1-2 предложения"}}

Заголовок: {title}
Текст: {body}
"""

PROMPT_TRANSLATE = """Переведи текст ниже на русский и адаптируй для канала о путешествиях.
Сохрани все факты, числа, названия, имена.

Верни ТОЛЬКО валидный JSON. Первый символ {{, последний }}.
Формат: {{"title": "переведённый заголовок", "text": "текст", "teaser": "1-2 предложения"}}

Заголовок: {title}
Текст: {body}
"""

RUBRIC_PROMPTS = {
    "Блюда и кухня": PROMPT_FOOD,
    "Достопримечательности": PROMPT_LANDMARK,
    "Интересные факты": PROMPT_FACT,
}

RUBRIC_HASHTAGS = {
    "Блюда и кухня": "#ТутИТам #еда #кухня #гастрономия",
    "Достопримечательности": "#ТутИТам #достопримечательности #туризм",
    "Интересные факты": "#ТутИТам #факты #интересное",
}


def get_prompt_for_rubric(rubric):
    return RUBRIC_PROMPTS.get(rubric, PROMPT_FACT)


def get_hashtags_for_rubric(rubric):
    return RUBRIC_HASHTAGS.get(rubric, "#ТутИТам #путешествия")
